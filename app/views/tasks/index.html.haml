%h1 Listing tasks

%table
  %tr
    %th Sprint
    %th User
    %th Title
    %th Description
    %th Estimate
    %th Remaining time
    %th
    %th
    %th

  - @tasks.each do |task|
    %tr
      %td= task.sprint
      %td= task.user
      %td= task.title
      %td= task.description
      %td= task.estimate
      %td= task.remaining_time
      %td= link_to 'Show', task
      %td= link_to 'Edit', edit_task_path(task)
      %td= link_to 'Destroy', task, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Task', new_task_path
