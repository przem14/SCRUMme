%h1
  = 'Team ' + @team.name


%ul.list-group
  %li.list-group-item.active Members
  - @team.users.each do |user|
    %li.list-group-item
      = user.email

= link_to 'Add member', new_member_path(@team)
= link_to 'Edit', edit_team_path(@team)
= link_to 'Back', teams_path